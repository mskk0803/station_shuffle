通知だよ～～
<% @notifications.each do |notification| %>
  <div class="notification">
    <% if notification.follow? %>
      <%= link_to notification.notifiable.follower.name, profile_path(notification.notifiable.follower) %> さんがあなたをフォローしました。
    <% elsif notification.like? %>
      <%= link_to notification.notifiable.user.name, profile_path(notification.notifiable.user) %> さんがあなたの投稿にいいねしました。
    <% end %>
  </div>
<% end %>
